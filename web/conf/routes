# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page

GET     /                               controllers.Application.index

GET     /properties                       controllers.Application.properties

GET     /viewProperties                      controllers.Application.viewProperties

GET     /generate                       controllers.Application.generate

GET     /generatorStatus              controllers.Application.generatorStatus

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

POST     /properties               controllers.Application.setProperties